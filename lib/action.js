// const department = require('./department');
// const employee = require('./employee');
// const role = require('./role');

// function action(actions) {
//     if (`${actions}` === 'View All Employees' || `${actions}` === 'Add Employee' || `${actions}` === 'Update Employee Role') {
//         return employee(actions);
//     } else if (`${actions}` === 'View All Roles' || `${actions}` === 'Add Role') {
//         return role(actions);
//     } else if (`${actions}` === 'View All Departments' || `${actions}` === 'Add Department') {
//         return department(actions);
//     }
// }

// module.exports = action;